sudo: required
services:
  - docker
before_script:
  - sudo iptables -L
  - sudo service docker restart ; sleep 10
  - sudo iptables -L
  - docker pull pascalgrimaud/tomcat
script:
  - sudo iptables -L
  - service docker restart ; sleep 10
  - sudo iptables -L
  - docker version
  - docker info
  # - sudo iptables --list
  # - docker run -d -p 8080:8080 pascalgrimaud/tomcat ; sleep 10
  # - docker run -d -p 127.0.0.1:8081:8080 pascalgrimaud/tomcat ; sleep 10
  # - docker run -d -p 0.0.0.0:8082:8080 pascalgrimaud/tomcat ; sleep 10
  # - docker run -d pascalgrimaud/tomcat ; sleep 10
  # - docker ps -a
  # - docker logs jhipster
  # - curl --retry 10 --retry-delay 5 -I http://localhost:8080/ | grep "HTTP/1.1 200 OK"
