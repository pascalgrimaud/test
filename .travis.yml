os:
  - linux
services:
  - docker
language: node_js
node_js:
  - "4.4.4"
jdk:
  - oraclejdk8
install:
  - npm install -g npm yo bower gulp-cli
script:
  - java -version
  - git --version
  - mvn -v
  - node -v
  - npm -v
  - bower -v
  - yo --version
  - gulp -v
  - docker version
  - docker-compose version
  - cd "$HOME"/generator-jhipster
  - npm install
  - npm link
  # - sh test.sh
