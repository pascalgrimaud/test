machine:
  services:
    - docker
  java:
    version: oraclejdk8
dependencies:
  override:
    - java -version
    - git --version
    - mvn -v
    - node -v
    - yo --version
    - npm -v
    - bower -v
    - grunt --version
    - gulp -v
    - grunt --version
    - docker version
    - docker-compose version
test:
  override:
    - echo "hello"
