machine:
  services:
    - docker
dependencies:
  override:
    - java -version
    - git --version
    - yo --version
    - mvn -v
    - node -v
    - npm -v
    - bower -v
    - grunt --version
    - gulp -v
    - grunt --version
    - docker version
    - docker-compose version
test:
  override:
    - echo "hello"
